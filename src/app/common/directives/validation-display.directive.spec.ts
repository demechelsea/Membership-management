import { ValidationDisplayDirective } from './validation-display.directive';

describe('ValidationDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
